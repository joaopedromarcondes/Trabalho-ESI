<h1>Enviar áudio de ruído</h1>

<% if flash[:alert].present? %>
  <div class="alert"><%= flash[:alert] %></div>
<% end %>

<%= form_with url: audio_submissions_path, scope: :audio_submission, local: true, multipart: true do |f| %>
  <div>
    <label>Áudio</label>
    <button type="button" id="start-recording">Iniciar Gravação</button>
    <button type="button" id="stop-recording">Parar Gravação</button>
  </div>

  <div>
    <label>Latitude</label>
    <%= f.text_field :latitude %>
  </div>

  <div>
    <label>Longitude</label>
    <%= f.text_field :longitude %>
  </div>

  <div>
    <label>Duração (segundos)</label>
    <%= f.hidden_field :duration_seconds %>
  </div>

  <div>
    <%= f.submit 'Enviar' %>
  </div>
<% end %>
